manifest_ref: core/manifest.yml

runtime:
  adapter: python
  scripts_dir: adapters/python/scripts
  workflow_config: core/config/workflow.yml
  stages_dir: workspace/stages

metadata:
  created_at: "2026-04-23"
  owner: "team"
  notes: "Runtime metadata for the default Python adapter."
